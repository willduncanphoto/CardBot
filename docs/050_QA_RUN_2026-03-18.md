# 050 QA Run — 2026-03-18

Environment: local dev machine, isolated HOME for most command checks (`/tmp/cardbot-qa-home.*`).

## Automated / Scripted Results

### ✅ A. Test + Build + Status smoke
Command:
```bash
make qa-050
```
Result: PASS

### ✅ B. LaunchAgent install/uninstall flow
Commands:
```bash
HOME=/tmp/cardbot-qa-home.nrshnI ./cardbot install-daemon
HOME=/tmp/cardbot-qa-home.nrshnI ./cardbot daemon-status
HOME=/tmp/cardbot-qa-home.nrshnI ./cardbot uninstall-daemon
HOME=/tmp/cardbot-qa-home.nrshnI ./cardbot daemon-status
```
Result: PASS
- Install created plist and loaded service.
- Uninstall removed plist and disabled loaded state.

### ✅ C. Daemon status output
Commands:
```bash
./cardbot daemon-status
./cardbot daemon-status --json
```
Result: PASS
- Text output includes daemon, single-instance guard, launch agent sections.
- JSON includes: `version`, `pid`, `daemon`, `single_instance_guard`, `launch_agent`.

### ✅ D. Daemon start/stop lifecycle
Method: start daemon, send SIGINT after ~2s.
Result: PASS
- Observed `CardBot daemon started — watching for cards...`
- Observed `Daemon shutting down`

### ✅ E. Direct path targeting
Command pattern:
```bash
./cardbot /tmp/.../Volumes/QA_CARD
```
Result: PASS
- Observed immediate target path analysis message: `"..." (target)`

---

## Manual-Only Checks (Pending)

These require real hardware or macOS permission toggles and were not fully automated:

### ⏳ Sleep/Wake card behavior
- Validate no launch storm after wake with real card insert/remove cycles.

### ⏳ Automation permission hints
- Revoke Automation permission and confirm daemon launch hint quality.

### ⏳ Full Disk Access permission hints
- Restrict access and confirm daemon launch hint quality.

### ⏳ Interactive setup full-path choices
- Piped stdin setup automation is not reliable due mixed native/fallback destination prompt behavior.
- Needs human-run `cardbot --setup` confirmation for:
  - daemon enabled = yes
  - start at login = yes
  - terminal app selection persistence

---

## Overall Gate Status

- Automated checks: ✅ PASS
- Manual hardware/permission checks: ⏳ PENDING

Recommendation: perform pending manual checks before tagging final v0.5.0 release.
